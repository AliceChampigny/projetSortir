
{% extends 'base.html.twig' %}

{% block title %}Connexion{% endblock %}

{% block body %}
{#    <form method="post">#}


        {% if app.user %}
            <div class="mb-3">
                Vous êtes connecté(e) en tant que {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Se déconnecter</a>
            </div>
        {% endif %}
        <section class="section_form">
            <h1 class="h3 mb-3 font-weight-normal">Veuillez saisir vos identifiants</h1>
    {% if error %}
        <div class="alert alert-danger" >{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}
            {#    <label for="inputPseudo">Pseudo</label>#}
            {#    <input type="text" value="{{ last_username }}" name="pseudo" id="inputPseudo" class="form-control" autocomplete="username" required autofocus>#}
            {#    <label for="inputPassword">Password</label>#}
            {#    <input type="password" name="password" id="inputPassword" class="form-control" autocomplete="current-password" required>#}

{#            <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">#}

            {#    <button class="btn btn-lg btn-primary" type="submit">Sign in</button>#}
{#    </form>#}
    <main>

        <section class="section_form">
            <form method="post" id="consultation-form" class="feed-form" action="#">
                <input required="" name="email_or_pseudo" placeholder="E-mail ou Pseudo" type="text" value="{{ last_username }}">

                <input name="password" required="" placeholder="Mot de Passe" type="password">
                <button type="submit" class="submit">Se Connecter</button>
                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
            </form>
            <div class="checkbox mb-3">
                <label>
                    <input type="checkbox" name="_remember_me"> Se souvenir de moi
                </label>
            </div>
        </section>
    </main>
    <div class="flex">
        <a href="{{ path('app_forgot_password_request') }}">Mot de passe oublié ?</a>
    </div>
{% endblock %}