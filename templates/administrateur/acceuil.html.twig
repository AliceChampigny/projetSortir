{% extends 'base.html.twig' %}

{% block body %}
    <a href="{{ path('admin_gestionutilisateur') }}">
        <button class="submit">GERER LES UTILISATEURS</button></a>
    <a href="{{ path('admin_gestionSorties') }}">
        <button class="submit">GERER LES SORTIES</button></a>
{% endblock %}