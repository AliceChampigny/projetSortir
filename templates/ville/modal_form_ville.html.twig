{% block body %}
    <div id="create_ville">
        {{ form_start(villeForm,{'attr':{'id': 'ville_form','class':'ville_modal'},'action': path('ville_ajoutville') }) }}
        {{ form_row(villeForm.nom,{'attr':{'hidden':true}}) }}
        {{ form_row(villeForm.codePostal,{'attr':{'hidden':true}}) }}
        <button class="button_submit">Ajouter cette ville</button>
        {{ form_end(villeForm) }}
    </div>
{% endblock %}
